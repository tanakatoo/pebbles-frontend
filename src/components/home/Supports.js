import React from 'react'
import { Button } from '../button/Button'

function Supports({ img, bgColor, font, text }) {
  return (
    <>
      <div className="mx-auto">{icon}</div>
      <p className="text-center mt-6 mx-12">{text}</p>
    </>
  )
}

export default Supports