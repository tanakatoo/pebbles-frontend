
import React from 'react'
import { Tab } from '@headlessui/react'
import ProfileTabProfile from './ProfileTabProfile'
import dbText from "../../text/db.json"
import { Link } from 'react-router-dom'

function ProfileTabContainer({ pageText, currentProfile, lang }) {

    console.log('profiletabcontainer', pageText)
    return (
        <></>
    )
}

export default ProfileTabContainer